---
description: Demonstrates script features including variables, heredocs, and capture
argument-hint: <topic> [detail-level]
---

# Simple prompt with inline text
prompt("List 3 facts about $1") -> $facts

# Use heredoc for multi-line prompt
prompt(<<EOF
Given these facts:
$facts

Summarize them in one sentence.
EOF
) -> $summary

# Reference the last output with $_
prompt("Translate to Spanish: $_")

# Use captured variables in later prompts
prompt("Original facts: $facts\n\nSummary: $summary\n\nAdd one more interesting detail.")
